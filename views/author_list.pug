extends layout

block content
  h1= title

  if author_list.length
    ul
      each author in author_list
        li
          a(href=author.url) #{author.name}
          |  (#{author.birthDate_formatted} - #{author.deathDate_formatted})


        //-   a(href=author.url) #{author.name}
            //-    if author.birthDate_formatted 
            //- |   (#{author.birthDate_formatted})





  else
    p There are no authors.
